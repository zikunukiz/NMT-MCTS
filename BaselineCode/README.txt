Here is the code to replicate the baseline models we used "Policy+RL' as well
as the actor-critic model. All of these are the files starting with 'train' and
can run them to train those models. May have to create some new directories for
the saved models that those files rely on. 
